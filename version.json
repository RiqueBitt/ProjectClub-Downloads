{"version": "v0.0.61", "notes": "- Corrige o erro real ao abrir o perfil: violação das Regras dos Hooks do React. O 'if (!userId) return null;' que já existia no componente ficava ANTES de vários useState/useEffect que adicionei nas últimas duas respostas (traços, visitantes, relacionamento, álbum) — quando o modal estava fechado (userId nulo), esses hooks nunca eram chamados; quando abria um perfil de verdade, passavam a ser chamados. O número de hooks mudando entre renderizações quebra o React de forma fatal. Corrigido movendo o próprio guarda pra logo antes do JSX de retorno, depois de TODOS os 38 hooks do componente (confirmado um por um) — em vez de reordenar múltiplos blocos de hooks espalhados pelo meio do arquivo"}
{"version": "v0.0.85", "notes": "- 2 correções pequenas:\n- Corrige o erro de deploy: 'Added the required column updatedAt to the UpdateLogEntry table without a default value. There are 25 rows...'. @updatedAt sozinho não conta como valor padrão pras linhas JÁ existentes na tabela quando o Prisma aplica a migração — precisa de @default(now()) explícito junto. As 25 atualizações já publicadas ganham 'agora' como valor inicial nesse campo; continua atualizando sozinho a cada edição normalmente, graças ao @updatedAt. Confirmado que os outros 2 lugares do schema com esse mesmo padrão (RelationshipRequest, Ticket) não foram tocados — já existiam desde antes desta sessão e nunca passaram por esse problema (não precisavam da mesma correção)"}